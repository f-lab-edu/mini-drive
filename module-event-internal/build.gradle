plugins {
    id 'convention.spring-boot-starter'
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":domain"))
}
