plugins {
    id 'convention.spring-boot-starter'
    id 'convention.java-toolchain'
    id 'convention.kafka'
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}

repositories {
    mavenCentral()

}